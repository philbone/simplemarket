os:
  - linux

language: php

php:
  - '7.1'

before_script:
  - mysql -e "create database IF NOT EXISTS apirest;" -u homestead -p secret
  - php artisan key:generate
  - php artisan migrate
  - php artisan db:seed

